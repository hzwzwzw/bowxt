# bowxt 0.4.0 验证记录

验证日期：2026-08-20（Asia/Shanghai）

## 验证环境

- 宿主：Fedora Linux / GNOME Wayland
- 容器：固定摘要的 Ubuntu 24.04 amd64
- 微信：官方 Linux 4.1.1.8
- 图形栈：Xvfb + XFCE + x11vnc + noVNC
- 自动化：只读 AT-SPI + XTest 真实输入 + 临时剪贴板
- 持久化：SQLite WAL

## 自动化与容器验证

| 项目 | 结果 | 证据 |
|---|---|---|
| 单元测试 | 通过 | 宿主与运行容器内共 84 项测试全部通过，且将 `ResourceWarning` 视为错误 |
| Shell 与 Python 静态检查 | 通过 | `bash -n manage.sh scripts/*`、`compileall` 通过 |
| 容器健康检查 | 通过 | Xvfb、桌面、fcitx5、微信、x11vnc、noVNC、Web 服务均运行 |
| Web / noVNC | 通过 | 8787 Web IM 与 6080 noVNC 页面返回 HTTP 200 |
| 微信控件树 | 通过 | 登录后 `doctor` 找到微信主窗口、会话列表、消息列表和可编辑输入框 |
| fcitx5 拼音 | 通过 | 安装拼音扩展、GTK3/Qt5 前端，进程运行，默认输入法配置为 `pinyin` |
| 单窗口 VNC | 通过 | `VNC_SCOPE=window` 仅转发微信顶层窗口；桌面模式可选 |
| VNC 独立性 | 通过 | 停止 x11vnc/noVNC 后，Xvfb、微信及收发逻辑继续运行并完成已验证发送 |
| 安全锁恢复 | 通过 | 临时窗口检查仍令当前操作失败；工作线程随后丢弃已锁客户端并重新连接，不重放失败操作 |

`fcitx5-remote` 在没有获得焦点的输入框时可能返回 `0`，表示输入法处于关闭态，而不是守护进程未运行。
自动化发送不依赖当前输入法状态。

## 三会话实机验证

本轮只使用任务明确授权的两个联系人和一个测试群，文档中以“联系人 A”“联系人 B”“测试群”代称。

| 项目 | 结果 | 证据 |
|---|---|---|
| 多会话读取 | 通过 | 三个会话均成功读取可见消息并分别持久化 |
| 并发提交 | 通过 | 三个 HTTP 客户端同时提交发送；单 UI 工作线程依次执行，三条回执均 `verified=true` |
| 会话类型隔离 | 通过 | 两个联系人保持 `contact`，测试群保持 `group`，轮询不会互相覆盖 |
| 持续轮询去重 | 通过 | 30 秒轮询前后均为 3 个会话、12 条消息、12 个唯一消息 ID，序号边界不变 |
| 服务重启恢复 | 通过 | Web/UI 服务重启后自动恢复连接，消息计数保持不变 |
| Web IM | 通过 | 三个会话均显示各自最新消息预览；切换、SSE 状态、文本气泡和发送区正常 |
| Agent 增量消费 | 通过 | 新 consumer 不回放历史；同轮两条新群消息一次 claim 返回，租约 NACK 后 attempt 递增并可 ACK |
| 群消息元数据 | 通过 | 真实资料卡补全两条新消息发送人；配置 `BOWXT_MY_NAMES=kirotta` 后 `@kirotta` 判为 `is_at_me=true` |

## Web IM 调度与时间改进

- 真实服务暂停后状态为 `paused=true`、活动会话为空，输入框及发送按钮禁用；恢复后继续轮询。
- 速度滑条通过 `/api/control` 将实时轮询间隔从 2.0 秒调整到安全下限 1.5 秒。
- 左下角键鼠间隔通过 `/api/control` 独立调整 XTest 事件与粘贴等待；60 ms 下限不会改变
  1.8 秒发送限频、分钟限额或随机抖动。
- 消息区与会话列表使用独立的有界滚动容器；状态事件只更新标题状态，不再重建列表并把用户
  拉回底部。非当前会话收到新的入站 SSE 消息时显示红点，打开后清除。
- 隔离浏览器夹具加载 80 条消息后，消息容器保持 469 px 可视高度、8286 px 内容高度和
  `overflow:auto`；向上滚动至 6017 px 后触发状态事件，位置仍为 6017 px。另一会话随后收到
  入站事件，红点出现并在打开会话后清除。该测试同样未操作真实微信。
- 隔离假微信后端将单次发送延迟固定为 2.5 秒；浏览器在第一条仍发送时成功提交第二条，两个
  气泡立即出现并同时显示旋转状态，输入框始终可用，随后后台按序完成且队列归零。该测试没有
  向真实联系人发送消息。
- 当前真实群聊中，对全部可见入站消息逐条打开资料卡补昵称的一次基准耗时为 40.38 秒；窗口
  像素方向采样仅为 0.067 秒。现实现会补全当轮所有新入站消息并缓存；没有新消息时才每轮追补
  一条历史缺失昵称的记录。发送回显验证完全跳过资料卡。昵称到达后按原 `seq` 更新，SQLite 不新增副本。
- 服务状态保存最近一次发送的 `queue_wait_s`、`open_chat_s`、`input_to_enter_s`、`verify_s` 和
  `end_to_end_s`，Web 左下角直接显示这些分段耗时。
- 发送与群昵称补全现在使用高低优先级队列。隔离测试在第一张资料卡未退出时加入发送，验证执行顺序为
  `资料卡 1 -> 发送 -> 资料卡 2`。
- 红框 `test` 的实机 AT-SPI 行位于消息视口上方，仅最后 1 像素与视口相交，但 Qt 仍标记为 `showing`。
  现实现要求有意义的可见高度，边界残留不再入库。
- noVNC 链接启用 `reconnect=1` 与 1000 ms 重连，用于微信登录窗口销毁后 x11vnc 跟随新主窗口的短暂断开。
- 当前微信 AT-SPI 消息行没有时间属性或时间子节点；只存在独立的时间分隔行，后续多个消息行
  共用它。Web IM 已改为每组仅显示一个时间条，不再为每个气泡展示伪精确时间。
- “wx4linux 端到端测试”在本地 SQLite 中存在两个不同 ID，但实时微信控件树不再返回该内容。
  根因是 Qt 虚拟列表一度以“带时间分隔/不带时间分隔”两种形式暴露同一行。新实现过滤列表
  视口外和 offscreen/defunct 控件，并在一个变体缺失时间时按语义与可见序号合并。两条错误缓存
  已从 bowxt 数据库删除；操作前一致性备份位于
  `/home/wechat/.local/share/bowxt/messages-pre-webim-improvements-20260819.db`。
- 群聊中的两条旧“可爱捏”再次进入可见区域时，时间分隔行已被虚拟列表移除，解析结果同时缺少
  时间和发送者；数据库原记录已由资料卡补全发送者，因此原先的强发送者条件无法命中。现去重逻辑
  仅在群聊入站消息的一侧发送者未知时允许匹配，并继续用同文可见序号区分多个相同气泡；两条重复缓存
  已删除，操作前一致性备份位于
  `/home/wechat/.local/share/bowxt/messages-pre-sender-time-reconcile-20260819.db`。

## 图片消息读取

- `hzw` 会话中的真实图片行在 AT-SPI 中表现为 `list item name="图片"`、整行边界
  `704×270`，没有子控件、文件路径或原始附件接口。
- bowxt 对该行做一次微信窗口截图，以背景色差的最大连通区域定位图片，实测排除了左侧
  `35×35` 头像，得到逻辑图片区域 `226×249`；按物理窗口像素编码出的 PNG 为 `236×260`、
  约 72 KiB，画面边界干净且没有聊天文字、头像或输入区。
- 普通点击定位后的图片区域会打开一个独立的 WeChat 顶层窗口；X11 `WM_CLASS` 仍为 `wechat`，
  AT-SPI 标题精确为“图片和视频”。在该窗口执行 Ctrl+C 后，剪贴板声明 `image/png`、
  `image/jpeg`、`image/bmp` 等格式；优先读取的 PNG 实测为 `1200×2670`、688862 字节，明显高于
  同一气泡的 `110×234` 预览。随后按 Esc，查看器窗口消失、主窗口重新激活且 `hzw` 会话仍打开。
- 持续服务先持久化预览，不阻塞 Web IM；在发送队列为空时才执行查看器复制，并以
  `image_source=viewer_clipboard` 原位替换 `window_pixels` 版本。发送命令始终排在图片任务之前，
  图片 URL 带 SHA-256 查询参数，替换后浏览器不会继续使用旧的 immutable 缓存。
- 真实消息被解析为 `type=image`、`content=[图片]`、`direction=incoming`；隔离数据库成功保存
  PNG、SHA-256、尺寸和 `/api/messages/{seq}/image` URL。全过程未使用 OCR、微信协议、缓存目录
  扫描或控件值注入。
- 单元测试覆盖无文字图片气泡解析、头像排除、窗口像素裁剪、剪贴板 PNG 尺寸验证、预览到原图
  的存储升级、后台图片任务及 HTTP 图片端点。
- 图片行仅剩一部分可见、主体定位失败时不会回退保存整行截图；图片文件也会在消息的时间/发送者
  变体完成合并后才落盘，避免虚拟列表重绘产生孤儿 PNG。
- 正式持续服务在 `hzw` 会话中保存了两条图片记录：`seq=71` 暂保留 `236×260` 预览（当前主体
  未完整可见，不做歧义点击），`seq=72` 已从 `110×234` 预览升级为 `1200×2670`、688862 字节
  的查看器 PNG；被替换的旧预览文件没有留下孤儿引用。
- 两个 `/api/messages/{seq}/image` 地址均返回 `200 image/png`；浏览器验收确认 Web IM 为两条
  消息分别创建了可点击链接和实际 `img` 元素，会话预览显示 `[图片]`。

实机持续轮询暴露过一个重要问题：微信虚拟控件行会在重绘后改变 AT-SPI 节点身份。旧算法把节点身份放进消息哈希，
会把同一条消息重复入库。现实现只使用稳定语义字段与可见列表中的同文序号生成 ID；新增的节点重建、同文重复消息测试
均已通过。修复前的 bowxt 数据库保留在容器卷中的
`/home/wechat/.local/share/bowxt/messages-pre-stable-id-20260819.db`，没有改动微信自身数据。

## 新消息唤醒模式

- 实机侧边栏未读行直接由 AT-SPI 暴露为 `list item name="hzw 1条未读 测试 23:10"`；无需
  截图、OCR 或颜色识别。读取后同一行变为 `hzw 测试 23:10`，未读文本消失。
- 服务切换为 `mode=unread` 后因该标记打开 `hzw`，将新消息“测试”保存为 `seq=74`，时间为
  `23:10`；随后状态回到 `active_chat=null`、`queue_depth=0`，没有继续轮换其他会话。
- 唤醒扫描一次向客户端请求最多 32 个可见未读行；客户端逐行等待聊天标题实际变化后再读取下一
  行，服务在同一周期保存全部已发现会话。单元测试同时覆盖双未读行和安静会话不被轮询。
- 当前打开的已监听会话没有红点时会原地读取消息列表，以避免微信不为当前会话显示未读标记造成
  漏读；这个路径不发送点击、不切换会话。

## Web 与持久化验收

- Web API 支持状态、会话增改、历史消息、最新预览、发送和 SSE 事件流。
- Web 线程不会直接操作微信；全部 UI 工作进入同一个串行队列。
- Web UI 使用 `textContent` 渲染会话名与消息正文，并设置 CSP，不以 `innerHTML` 注入用户内容。
- SQLite 每次操作都关闭连接，启用 WAL，并以 `(chat_id, message_id)` 唯一约束去重。
- 已验证发送回执会与随后观察到的微信气泡合并，不保留两条副本。

## 安全边界与已知限制

- 没有微信协议发包、私有 API、数据库解密、进程注入、内存扫描、Hook、深层控件写入或 OCR。
- 方向判断只分析气泡左右几何；不会识别窗口图像中的文字。
- 群发送者昵称在当前微信版本的消息行控件树中通常不存在。可选资料卡路径通过 AT-SPI 读取昵称，并在继续输入前验证资料卡已退出；持续服务默认关闭该路径以减少界面扰动。
- 一个微信窗口物理上只能激活一个会话，因此外部请求可以并发提交，实际 UI 操作仍由单线程安全串行化。
- “持续监听”只处理微信当前能够渲染的可见消息和侧边栏明确标记的可见未读会话，不等同于导出完整历史。
- 端口默认只绑定 `127.0.0.1`；远程访问应使用 SSH 隧道。

调试截图、聊天数据库、登录数据和实际会话名称均不进入仓库或发布包。
