#!/usr/bin/env bash
set -euo pipefail

if [[ ! -s /run/bowxt/session.env ]]; then
  echo "desktop session is not ready" >&2
  exit 1
fi
# shellcheck disable=SC1091
source /run/bowxt/session.env
exec "$@"
